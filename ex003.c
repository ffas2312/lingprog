#include <stdio.h>

int main(int argc, char *argv[])
{
	int x, y;
	scanf("%d", &x);
	scanf("%d", &y);
		printf("O maior numero digitado foi "\n);
		if(x > y){
			printf("%d\n", x);
					} else {
						printf("%d\n", y);
					}
					return 0;

} 